// Name: Shreya Kumari 

/* Pseudocode:

- create a react component (userPage)
- create a state object to hold the inputs (current email address, new email address, current phone number, & new phone number)
- create a function to update the states based on user input  
- create a function to validate user input (checks for invalid or empty inputs)
- integrate submit button to run the validate function
- utilize user page UI for extra info for page such as account creation date, events added, etc.

*/

/* TEST CASE CODE --> 

const validateForm = () => {
    if (!formData.currentEmail.trim()) {
        alert("Current Email Address is required.");
        return false;
    }
    if (!formData.newEmail.trim()) {
        alert("New Email Address is required.");
        return false;
    }
    if (!formData.currentNumber.trim()) {
        alert("Current Phone Number is required.");
        return false;
    }
    if (!formData.newNumber.trim()) {
        alert("New Phone Number is required.");
        return false;
    }
    alert("Form submitted successfully!");
    return true;
};

*/

